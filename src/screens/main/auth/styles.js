const React = require( "react-native" );

const { StyleSheet } = React;
export default StyleSheet.create( {
    loginForm: {
        alignItems: "center",
        marginBottom: 100,
        marginLeft: 20,
        marginRight: 20
    }
} );
